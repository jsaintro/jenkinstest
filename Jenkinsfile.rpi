pipeline {
    agent { docker 'hypriot/rpi-python' }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
                sh 'pwd'
                sh 'hostname'
            }
        }
    }
}
